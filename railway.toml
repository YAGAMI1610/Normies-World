[build]
builder = "nixpacks"
buildCommand = "npm install && npx prisma generate --schema=apps/server/prisma/schema.prisma && npm run build && npm run build --workspace=apps/web"

[deploy]
startCommand = "node apps/server/dist/app.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
