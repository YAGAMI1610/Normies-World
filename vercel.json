{
  "version": 2,
  "projects": [
    {
      "name": "normies-alpha-web",
      "src": "apps/web"
    }
  ],
  "buildCommand": "npm run build --workspace=packages/shared-types && npm run build --workspace=packages/contracts && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/server/:path*",
      "destination": "https://normies-alpha-server.railway.app/api/:path*"
    }
  ]
}
